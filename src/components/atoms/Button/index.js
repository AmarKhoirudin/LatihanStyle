import React from "react";
import "./index.css"

const Button = ({ titlle }) => {
  return (
    <button
      style={{
       
      }}
      className="ButtonComponents"
    >
      {titlle}
    </button>
  );
};

export default Button;
